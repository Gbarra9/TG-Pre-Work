const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below

fruits.forEach((fruit) => console.log(`I want to eat a ${fruit}`))

// forEach method executes code for each element in the array
// .forEach takes in a callback function (a function passed as an argument into another function)
