// Write your fasterShip object literal below

let fasterShip = {
  color: 'silver',
  'Fuel Type': 'Turbo Fuel'
}

// create an object literal using curly braces {}
// an object is filled with unorganized data in the form of kev value pairs
// key is similar to variable name that points to location in memory that holds a value
// key value can be any type of JS data type, key value pairs are separated using commas
// keys are strings, if string has any special characters string has to have quotes on it (no quotes otherwise)
