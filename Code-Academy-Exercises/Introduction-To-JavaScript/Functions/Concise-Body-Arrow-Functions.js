const plantNeedsWater = day =>
day === 'Wednesday' ? true : false;

// Concise  body means using the most condensed form of arrow functions

// functions that take single parameter can have paranthses that wrap parameter to be omitted
// only 0 and multi parameters require ()

// function body composed of single line can omit return and curly braces
// multi line code block requires both return keyword and curly braces
