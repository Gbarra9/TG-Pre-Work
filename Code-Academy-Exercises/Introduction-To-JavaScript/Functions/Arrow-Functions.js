const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};


// Arrow function is another way to declare a function

// arrow function uses () => to omit function keyword
// place parameters in parantheses followed with arrow => that points to function body {} 
