const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight'
  console.log(lightWaves)
}

logVisibleLightWaves()
console.log(lightWaves)

// Variable defined inside block called local scope
// local variables only accessible to code inside curly braces {}
// reference error occurs if variable accessed outside its scope cannot be accessed
