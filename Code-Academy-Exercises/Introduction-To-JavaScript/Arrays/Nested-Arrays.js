const numberClusters = [ [1,2], [3,4], [5,6] ]

const target = numberClusters[2][1]

// Arrays nested inside an arrays are called nested arrays
// use chain bracket notatiton to target an array's element inside an array
