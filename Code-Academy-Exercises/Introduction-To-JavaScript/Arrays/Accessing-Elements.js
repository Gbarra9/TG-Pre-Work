const famousSayings = ['Fortune favors the brave.', 'A joke is a very serious thing.', 'Where there is love there is life.'];

const listItem = famousSayings[0]
console.log(listItem)

console.log(famousSayings[2])

console.log(famousSayings[3])

// Arrays in JavaScript are zero-indexed, meaning the element list has the first element start at 0
// each element has a position in the array called an indexed

// access an element in an array by typing the name of the array followed with closed square brackets []
// Put the index of element inside []
