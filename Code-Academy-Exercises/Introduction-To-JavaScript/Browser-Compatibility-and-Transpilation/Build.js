"use strict";

var pasta = "Spaghetti"; // ES5 syntax

var meat = "Pancetta"; // ES6 syntax

var sauce = "Eggs and cheese"; // ES6 syntax

// Template literals, like the one below, were introduced in ES6
var carbonara = "You can make carbonara with " + pasta + ", " + meat + ", and a sauce made with " + sauce + ".";


// Call build from command line using: npm run build
// command runs npm build script in package.json

// Babel writes transpiled code (ES5 JS) to main.js (same name as original file) into a folder called lib
