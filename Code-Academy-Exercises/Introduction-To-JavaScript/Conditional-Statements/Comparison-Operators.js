let hungerLevel = 7
if(hungerLevel > 7) {
  console.log('Time to eat!')
} else {
  console.log('We can eat later!')
}

// Comparison operators compare two values and returns a true or false based on values
// list of comparison operators: > , >= , < , <= , === , !==
