let athleteFinalPosition = 'first place';

switch (athleteFinalPosition ) {
   case('first place') :
    console.log('You get the gold medal!')
    break
   case('second place') :
    console.log('You get the silver medal!')
    break
   case('third place') :
    console.log('You get the bronze medal!')
    break
   default :
    console.log('No medal awarded.')
    break
}

// Use switch statement to provide alternative syntax for else if statements

// switch initiates statement and followed by (), the condition will contain value each case will compare // TODO:

// case keyword checks if expression to right matches specified value that comes after it

// case checks until match, if no match found default statement will run

// break keyweord stops code from running and exits code block
