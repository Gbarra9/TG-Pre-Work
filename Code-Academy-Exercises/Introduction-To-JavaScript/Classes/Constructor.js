class Surgeon {
  constructor(name, department) {
    this._name = name
    this._department = department
  }
}

// JavaScript calls constructor method every time it creates a new instance of a class
// constructor accepts parameters
