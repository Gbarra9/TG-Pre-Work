let newVariable = 'Playing around with typeof.';

console.log(typeof newVariable);

newVariable = 1;

console.log(typeof newVariable);

// check data type of value to right using typeof operator
// typeof returns value's data type via a string
